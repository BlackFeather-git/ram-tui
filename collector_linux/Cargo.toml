[package]
name = "collector_linux"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Linux /proc memory and process collectors for ram-tui"

[dependencies]
serde = { version = "1", features = ["derive"] }
libc = "0.2"

[dev-dependencies]
tempfile = "3"
