[package]
name = "collector"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cross-platform system memory and process telemetry collectors for ram-tui"

[dependencies]
serde = { version = "1", features = ["derive"] }
libc = "0.2"
core_render = { path = "../core_render" }

[dev-dependencies]
tempfile = "3"
