[package]
name = "ui"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Theme engine, layout modes, and TUI rendering for ram-tui"

[dependencies]
core_render = { path = "../core_render" }
collector = { path = "../collector" }
libc = "0.2"
